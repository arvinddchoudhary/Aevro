# AEVRO Legacy Prototype

This folder preserves the previous implementation for reference during the
Phase 1 stack migration.

```txt
legacy/frontend-vite      React + Vite storefront prototype
legacy/backend-fastapi    FastAPI backend prototype
```

Do not add new production features here. New work should happen in the
top-level `frontend/` and `backend/` folders.
